# nat.startup

# Configurar IP da interface LAN

ip addr add 10.0.0.1/24 dev eth0
ip link set eth0 up

# Criar rota estática para rede de clientes

ip route add 192.168.0.0/24 via 10.0.0.2

# Criar masquerade (NAT)

iptables -t nat -A POSTROUTING -o eth1 -j MASQUERADE