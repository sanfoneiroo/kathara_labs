# r1.startup

# Configurar interface WAN

ip addr add 10.0.0.2/24 dev eth0
ip link set eth0 up

ip route add default via 10.0.0.1

# Condigurar interface LAN

ip addr add 192.168.0.1/24 dev eth1
ip link set eth1 up